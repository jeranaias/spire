# GCSS-MC Schema Alignment — Autonomous Work Order                                                                                                                                                       
  ## Who you are
  You are Replit Agent (Claude Opus 4.7) operating on the SPIRE codebase in this Replit workspace. SPIRE is a Marine Corps contested-logistics OS shipping on Fly.io at https://spire-mdm.fly.dev. Repo:
  `jeranaias/spire`. Master is what's deployed.

  ## Why we're doing this
  Modern Day Marine 2026 final demo is **April 30, 2026** — 48 hours away. The judges are MARCORLOGCOM CDAO and HQMC I&L: people who have actually used GCSS-MC.

  A SSgt acquired real **sanitized GCSS-MC export data** — 103,687 service-request headers, 98,211 parts rows, 38,109 due-in rows, plus three official schema dictionaries. The data is in `tmp/gcss-mc/`
  in this Repl:

  ```
  tmp/gcss-mc/
  ├── hashed_header.csv         (103,687 SR headers — 12 columns)
  ├── hashed_sr_parts.csv       (98,211 parts rows — 6 columns)
  ├── hashed_due_in.csv         (38,109 due-in rows — 81 columns)
  ├── sr_header_dict.csv        (44 column definitions)
  ├── sr_repair_part_dict.csv   (26 column definitions)
  └── due_in_data_dict.csv      (96 column definitions)
  ```

  If `tmp/gcss-mc/` is missing or empty, **stop and ask the user to upload the files before continuing**. Do not fabricate a substitute dataset — the whole point of this work is parity with these
  specific real files.

  A profiling pass already ran. Findings:

  | Concept | Real GCSS-MC | SPIRE today | Action |
  |---|---|---|---|
  | Defect codes | `FCON.CBB` (48%), `WPNS.CBB` (17%), `ELEC.INOP` (3%), `COMP.INOP` (2%), `DAD1.INOP` (2%), `FCON.OPTIC` (2%), `FCON.INOP` (1%), plus tail | `["NMAJ", "TRSM"]`, `["NMAJ", "TIRE"]`,
  `["NMAJ", "ENGN"]`, `["SAFE", "BRAK"]` | Replace vocab + format |
  | Priority | `06 B-Urgent` (51%), `05 B-Urgent` (19%), `13 C-Routine` (8%), `09 B-Urgent` (6%), `03 A-Critical` (3%), `02 A-Critical` (3%) | `"02"`, `"05"`, `"10"` numeric only | Add letter category +
  label suffix |
  | Echelon | numeric `1` / `2` / `3` / `4` (2=52%, 3=35%, 1=7%, 4=<1%) | `"Organizational"` / `"Intermediate"` text | Convert to numeric |
  | Service request type | `"Maintenance - CM"` (100% of these 103K rows) | field doesn't exist | Add field |
  | DIC (due-in) | `A0A` (69%), `A01` (21%), `A2A` (7%), `A21` (2%) | not tagged | Tag |

  The pitch outcome we're driving toward: judge says "wait, can SPIRE actually ingest a real GCSS-MC export?" and we drop the real `hashed_header.csv` (103,687 rows) into the SENTRY upload tab on stage
  and watch it process.

  ## The judging rubric (everything you do must defend a line)

  | Weight | Category | What this work delivers |
  |---|---|---|
  | 30% | Mission Impact | SPIRE is no longer "synthetic toy" — it's a system that ingests the real GCSS-MC schema unmodified. |
  | 25% | Technical Innovation | Schema-aligned synthetic generator + drop-in real-data ingest path. |
  | 20% | Usability & Design | SENTRY Upload tab gains a one-click "Load real GCSS-MC export" affordance. |
  | 15% | Security & Sustainability | Sanitized data only; full schema-mapping page documents the integration contract. |
  | 10% | Team Collaboration | Schema work and stage rehearsal can run in parallel — leaves the stage-pivot work alone. |

  ## Hard rules — non-negotiable

  1. **NEVER push to master.** Branch: `gcss-mc-schema-alignment`. One PR at the end against master. Do not auto-merge.
  2. **NEVER add Claude / Anthropic / AI / "Generated with" / "Co-Authored-By: Claude" attribution** to commits, code, or comments. No emojis.
  3. **NEVER disclose Thornveil IP.** Scan list before every commit:
      - `HawkStack`, `SGDR`, `WEM-Inverted`, `WEM-Diamond`, `FCOS`, `ConvNeXt`, `Anti-UAV-410`, `1.77 M`, `1.77M params`, `1.88 M`, `0.5795`, `82.95`, `82.12`, `SentryHawk`, `ForgeHawk`, `DepthHawk`,
  `CellHawk`, `WildHawk`, `ENFORCE pillar`, `five-pillar`, `5-pillar`, `spillage scanner`, `Argus spillage`, `Tribunal cross-check`, `Hallucination probe`, `Action Gate`, `backbone family`
      - Sanctioned mentions in LICENSE.md / README.md / CONTRIBUTING.md / PR template only — naming products without disclosing mechanics.
  4. **NEVER commit the real `hashed_*.csv` files to the public repo.** They live in `tmp/gcss-mc/` (already in `.gitignore` via `tmp/`). They are demo-time inputs, not source. If `tmp/` is not in
  `.gitignore`, add it before doing anything else.
  5. **DO commit the three data dictionary files** (`sr_header_dict.csv`, `sr_repair_part_dict.csv`, `due_in_data_dict.csv`) to `docs/gcss-mc-schema/` — those are public schema documentation, safe to
  ship.
  6. **NEVER touch `backend/auth.py` session middleware logic.** It's locked.
  7. **VERIFY ingest end-to-end via Playwright** against the live dev server. Don't claim "the upload works" without driving the file picker and observing the processed output.
  8. **Determinism is sacred.** The synthetic dataset must regenerate byte-identical from seed 42 after your changes. If you change a probability distribution, update the regen baseline in the same
  commit and document why.

  ## Persistence — use a plan file

  Before doing anything else, create `GCSS_ALIGNMENT_PLAN.md` at repo root with the work-package list below as checkboxes. Update it after every step. Every commit message references the WP it addresses.

  ## Branch hygiene

  - `git checkout -b gcss-mc-schema-alignment master`
  - One commit per WP (or per logical sub-step inside a WP).
  - Repo commit style: imperative subject ≤72 chars, sentence case, optional dashed bullets in body.
  - After each commit: `git push -u origin gcss-mc-schema-alignment` (first time) then `git push`.
  - Final PR: `gh pr create --base master --head gcss-mc-schema-alignment` with the body specified at the bottom.

  ## Tech context

  - Dataset generator: `dataset/` (Python). Entry point `dataset/main.py`, orchestrator. Equipment profiles: `dataset/data/equipment_profiles.json`. Faults / parts: `dataset/faults.py`. Supply / due-in:
  `dataset/supply.py`. Lifecycle / SR generation: `dataset/lifecycle.py`. Remarks: `dataset/remarks.py`. Personnel: `dataset/personnel.py`.
  - Backend: FastAPI on uvicorn port 8700. Routes in `backend/routes/`. SENTRY ingest: `backend/routes/sentry.py`. Integrations page: `backend/routes/integrations.py` +
  `frontend/src/views/IntegrationsView.tsx`.
  - SENTRY frontend: `frontend/src/views/sentry/UploadTab.tsx`, `ProcessingTab.tsx`, `ReviewQueueTab.tsx`.
  - Build: `cd frontend && npx tsc --noEmit && npm run build`. Backend tests: `python -m pytest backend/tests/`.
  - Dataset regen: `python -m dataset.main --seed 42 --out dataset/data/generated/`. Confirm fingerprint stays stable.
  - The 4 mock CAC identities for Playwright sign-in: Reyes (G-4), Kowalski (Maintenance Chief), Park (Security Manager), Hayes (MEF Commander).

  ---

  ## Work packages

  ### WP-0 — Confirm the inputs are present (gate everything)

  **GOAL:** Verify `tmp/gcss-mc/` has all six files. Profile each one. Write the actual observed distributions to `GCSS_ALIGNMENT_PLAN.md` so subsequent WPs have ground truth to align against.

  **STEPS:**
  1. Verify all six files exist in `tmp/gcss-mc/`. If any missing → stop and ask the user.
  2. Verify `tmp/` is in `.gitignore`. Add it if not.
  3. Run a quick profiling script (write it as `scripts/profile_gcss.py`):
     - For `hashed_header.csv`: row count, distinct DEFECT_CODE histogram (top 20), MASTER_PRIORITY_CODE histogram, ECHELON_OF_MAINT histogram, SERVICE_REQUEST_TYPE values, DEADLINED_DATE format.
     - For `hashed_sr_parts.csv`: row count, distinct SERVICE_ACTIVITY values, mean PARTS_CHARGE, null QUANTITY_REQUIRED count.
     - For `hashed_due_in.csv`: row count, distinct DIC histogram (top 10), distinct PRI_CD histogram, distinct ITEM_TYPE histogram, distinct DOC_STATUS histogram.
  4. Save the profile output to `tmp/gcss-mc/PROFILE.md` (gitignored — for your reference + to update the plan file).
  5. Update `GCSS_ALIGNMENT_PLAN.md` with the observed distributions.

  **ACCEPTANCE:** `tmp/gcss-mc/PROFILE.md` exists with concrete numbers per dataset. `GCSS_ALIGNMENT_PLAN.md` has the distributions copied in.

  **COMMIT:** `WP-0 GCSS-MC profile: real-data distributions captured for alignment baseline`

  (Note: the script `scripts/profile_gcss.py` IS committed — useful for re-profiling after changes. The output `tmp/gcss-mc/PROFILE.md` is NOT committed.)

  ---

  ### WP-1 — Defect-code vocabulary and format alignment

  **GOAL:** Replace SPIRE's `["NMAJ", "TRSM"]` array vocabulary with real GCSS-MC dot-joined codes (`FCON.CBB`, `WPNS.CBB`, etc.) and rebalance equipment-fault distributions to match the real ~65%
  fire-control + weapons skew.

  **FILES:**
  - `dataset/data/equipment_profiles.json` — for every entry's `faults[].defect_code` array, replace with a single dot-joined string drawn from the real vocabulary. Use the WP-0 profile as the
  authoritative distribution. The mapping should respect equipment type:
    - Wheeled tactical (JLTV, MTVR, LVSR, MRAP, TRAILER): primarily `FCON.*`, `WPNS.*` (mounted weapons stations have fire-control), `ELEC.INOP`, `BODY.CBB`, `COMP.INOP`
    - Tracked combat (M1A1, LAV-25, LAV-AT, M88A2): primarily `FCON.CBB`, `FCON.OPTIC`, `WPNS.CBB`, `WPNS.SAFDL`, `DAD1.INOP`
    - Aviation (MV-22B, CH-53E): `FCON.OPTIC`, `ELEC.INOP`, `COMP.INOP`, `WPNS.CBB`
    - Engineer / generator / radar (MEP_803A, AN/TPS-80, AN/TPQ-36, D7G, TRAM, ROWPU, HIMARS): `ELEC.INOP`, `ELEC.BTRY`, `COMP.INOP`, `FCON.CBB` (HIMARS), `WPNS.CBB` (HIMARS)
  - `dataset/faults.py` — anywhere `defect_code` is a tuple/list, change to a single string. Update typing annotations.
  - `dataset/lifecycle.py` — anywhere `defect_code_primary` / `defect_code_secondary` are written, replace with a single `defect_code: str`. Update SR record fields, including the `_new_sr_number` join
  logic if it depends on these.
  - `dataset/export.py` (or wherever CSV/JSON exports happen) — emit the joined string in the export.

  **KEEP:** internal SPIRE logic that conditions on defect-code primary (e.g., "is this a SAFE class fault?") — refactor to operate on the new string by splitting on `.` once.

  **ACCEPTANCE:**
  1. After regen with seed 42, run the comparison script: SPIRE's defect-code histogram top-10 should approximately match the real top-10. Acceptance: top-3 codes (FCON.CBB, WPNS.CBB, ELEC.INOP) account
  for ≥ 65% of records, matching real data within ±10pp.
  2. No record has a non-real defect code (validate against the union of codes observed in real data).
  3. `pytest backend/tests/` still green.

  **COMMIT:** `WP-1 Defect-code alignment: real GCSS-MC vocab and ~65% FC/weapons skew`

  ---

  ### WP-2 — Priority code alignment (FAD/IPG full format)

  **GOAL:** Change SPIRE's priority field from numeric-only (`"02"`, `"05"`, `"10"`) to real GCSS-MC `"NN A/B/C-Description"` format.

  **REAL TARGET DISTRIBUTION (from WP-0 profile):**
  - `06 B-Urgent` ~51%
  - `05 B-Urgent` ~19%
  - `13 C-Routine` ~8%
  - `09 B-Urgent` ~6%
  - `03 A-Critical` ~3%
  - `02 A-Critical` ~3%
  - `12 C-Routine` ~3%
  - `07 A-Critical`, `14 C-Routine`, plus tail ~7%

  **FILES:**
  - `dataset/supply.py` — `_priority_for_condition` returns the full `"NN X-Label"` string. Map condition impact to the real distribution:
    - `"Deadlined"` → weighted choice from `["02 A-Critical", "03 A-Critical"]`
    - `"Degraded"` → weighted choice from `["05 B-Urgent", "06 B-Urgent", "09 B-Urgent"]` skewed toward `06 B-Urgent`
    - `"Minor"` → weighted choice from `["13 C-Routine", "12 C-Routine", "14 C-Routine"]`
    - `"Supply"` → weighted choice from `["13 C-Routine", "12 C-Routine"]`
  - `dataset/lifecycle.py` — anywhere a hardcoded priority literal exists (`priority="10"`, `priority="02"`, etc.), update to the new string format. PMCS / scheduled SRs should default to `"13
  C-Routine"`.
  - `dataset/lifecycle.py:rng.choices(["ROUTINE", "PRIORITY", "URGENT"], ...)` lines — replace with the new string format.
  - Anywhere internal logic does `priority == "02"` comparisons, refactor to `priority.startswith("02")` or extract the numeric prefix once.

  **ACCEPTANCE:**
  1. After regen, SPIRE priority distribution matches real within ±10pp on the top 4 entries.
  2. No record carries the old numeric-only format.
  3. Frontend renders the full label correctly (check `frontend/src/views/pulse/RiskBoardTab.tsx` and any other priority-displaying surface — if a parser was assuming numeric-only, fix it).

  **COMMIT:** `WP-2 Priority alignment: NN A/B/C-Description format with real distribution`

  ---

  ### WP-3 — Echelon-of-maint numeric + SERVICE_REQUEST_TYPE field

  **GOAL:** Convert echelon from text to numeric. Add `SERVICE_REQUEST_TYPE` as a first-class field on every SR.

  **FILES:**
  - `dataset/data/equipment_profiles.json` — replace `maintenance_level: "Organizational"` with `maintenance_level_default: 2` (numeric). Map text → numeric: Organizational=2, Intermediate=3, Depot=4.
  (1=Owner-unit; rare in this dataset; reserve for SAFE-class quick fixes.)
  - `dataset/lifecycle.py` — add `service_request_type: str = "Maintenance - CM"` to the SR record dataclass. PMCS-style records get `"Maintenance - PM"`. The vast majority of generated SRs (corrective)
  get `"Maintenance - CM"`.
  - `dataset/lifecycle.py` — add `echelon_of_maint: int` field to the SR record. Populate from the equipment profile, with low probability of bumping up one level for complex faults (matches real data
  where ~35% land at echelon 3).
  - `dataset/export.py` and any frontend consumer — surface both new fields.

  **ACCEPTANCE:**
  1. Regen produces records where `service_request_type` is `"Maintenance - CM"` for corrective and `"Maintenance - PM"` for PMCS.
  2. `echelon_of_maint` distribution matches real (2=52%, 3=35%, 1=7%) within ±10pp.
  3. PULSE risk board and SENTRY surfaces still render correctly.

  **COMMIT:** `WP-3 Echelon numeric (1-4) + SERVICE_REQUEST_TYPE field on every SR`

  ---

  ### WP-4 — DIC tagging on due-in records

  **GOAL:** Tag every due-in / requisition record with a real Document Identifier Code matching the real distribution.

  **REAL TARGET (from WP-0):**
  - `A0A` ~69% (routine issue)
  - `A01` ~21% (routine issue, alt format)
  - `A2A` ~7% (issue from another supply system)
  - `A21` ~2%

  **FILES:**
  - `dataset/supply.py` — when generating a due-in record, tag with a DIC drawn from the real distribution. Add `dic: str` to the requisition dataclass.
  - Document number generation: real DoD doc-number format starts with the DIC; ensure SPIRE's generated `f"{asset.unit_uic}-{_julian(sr_date)}-{_next_sequence(asset.unit_uic):04d}"` is augmented OR a
  separate DIC field is added.
  - Reference: real `hashed_due_in.csv` shows the DIC as a separate column, not embedded in DOC_NBR. Match that schema.

  **ACCEPTANCE:**
  1. All requisitions have a `dic` field.
  2. DIC distribution within ±5pp of real top 4.

  **COMMIT:** `WP-4 DIC tagging on due-in records: real GCSS-MC distribution`

  ---

  ### WP-5 — Schema-parity export adapters

  **GOAL:** SPIRE can emit data in the EXACT real GCSS-MC schema (column names, order, types, null handling). Three new endpoints.

  **REAL HEADER COLUMNS (12, in this exact order):**
  ```
  SERVICE_REQUEST_TYPE, SR_NUMBER, DEFECT_CODE, PROBLEM_SUMMARY,
  DATE_RECEIVED_IN_SHOP, ECHELON_OF_MAINT, SERIAL_NUMBER, TAMCN,
  DEADLINED_DATE, MASTER_PRIORITY_CODE, OWNER_UNIT_ADDRESS_CODE, JOB_STATUS_DATE
  ```

  **REAL PARTS COLUMNS (6):**
  ```
  SR_NUMBER, SERVICE_ACTIVITY, RNSN, QUANTITY_REQUIRED, PARTS_CHARGE, DOCUMENT_NUMBER
  ```

  **REAL DUE-IN COLUMNS (81 total, but most rows null most fields).** For now, emit the 12 most-populated: `DOC_NBR, DIC, NSN_ORDERED, PRI_CD, PURPOSE_CD, ESTABLISHED_DT, MAX_STAT_DT, SUPP_ADD,
  SR_NUMBER, ITEM_TYPE, UNIT_PRICE, DOC_STATUS`. Other 69 columns can be null in the export.

  **FILES:**
  - New: `backend/routes/gcss_export.py` — three endpoints:
    - `GET /api/integrations/gcss-mc/header.csv` → SR header CSV in real schema
    - `GET /api/integrations/gcss-mc/parts.csv` → SR parts CSV in real schema
    - `GET /api/integrations/gcss-mc/due-in.csv` → due-in CSV in real schema
  - All three respect role scoping (already enforced via the session middleware).
  - Date format: Oracle-style `DD-MON-YY` (e.g., `27-JAN-24`) on the header dates. Reference real data exactly.
  - `OWNER_UNIT_ADDRESS_CODE`: hash the unit_uic so the synthetic looks isomorphic to the hashed real data (`owner_unit_address_code_<hash>`).
  - `SERIAL_NUMBER` and `TAMCN`: same hashing for isomorphism.

  **ACCEPTANCE:**
  1. `curl /api/integrations/gcss-mc/header.csv > /tmp/synth_header.csv && head -1 /tmp/synth_header.csv` returns the exact real header line.
  2. `python` quickly diffs the synth header CSV's column set against `tmp/gcss-mc/hashed_header.csv` — must be identical column-by-column.
  3. Sample-row schema compatibility: pick 5 random synth rows, manually verify they could appear in the real export without parsing differently.

  **COMMIT:** `WP-5 GCSS-MC schema-parity exports: header/parts/due-in CSV in exact real schema`

  ---

  ### WP-6 — SENTRY upload tab ingests real GCSS-MC exports

  **GOAL:** A judge can drop the actual `hashed_header.csv` (103,687 rows) into the SENTRY Upload tab on stage and watch it process. This is the demo's credibility moment.

  **APPROACH:**
  - The SENTRY Upload tab today expects SPIRE's internal CSV format. Add a content-sniff: if the first row matches the real GCSS-MC header signature
  (`SERVICE_REQUEST_TYPE,SR_NUMBER,DEFECT_CODE,PROBLEM_SUMMARY,...`), route through a new adapter that maps real columns → SPIRE's internal record shape, then feeds the existing classification pipeline.
  - The internal record shape is whatever the SENTRY processor consumes today — don't reshape that contract; reshape the input.

  **FILES:**
  - `backend/routes/sentry.py` — extend the upload handler. Detect schema by checking the first row against the real signature. If real, run through the new adapter; else use the existing path.
  - New: `backend/sentry_gcss_adapter.py` — `def real_export_to_internal(csv_rows) -> list[InternalRow]`. Maps DEFECT_CODE → SPIRE's defect-code field, PROBLEM_SUMMARY → remark text (the field SENTRY
  actually classifies), SERIAL_NUMBER + TAMCN → hashed asset references, etc.
  - `frontend/src/views/sentry/UploadTab.tsx` — add a button "Load real GCSS-MC export" beside "Load canonical demo dataset". The button:
    1. Triggers a file picker accepting `.csv`.
    2. On file select, posts to the existing upload endpoint.
    3. Backend detects and adapts; UI shows a banner: `Real GCSS-MC export — {N} service requests — schema-validated against published data dictionary`.
  - `frontend/src/views/sentry/ProcessingTab.tsx` — confirm the processing animation works at 100K-row scale. If it doesn't, sample to 1K and show a "10× sample for animation; full set processed in
  background" note.

  **HANDLING THE REAL FILE'S SIZE:**
  The real header is 103K rows. Don't try to render all 103K in the review queue — paginate. Display total count + first 100 + sampling controls.

  **ACCEPTANCE (Playwright):**
  1. Sign in as Reyes. Navigate to SENTRY Upload tab. Click "Load real GCSS-MC export". Upload `tmp/gcss-mc/hashed_header.csv` (the agent will need to upload via a file input — use the Playwright
  file-upload pattern).
  2. Within 5 seconds, see banner showing 103,687 records.
  3. Switch to Processing tab. See activity (animation, counters).
  4. Switch to Review Queue. See ≥1 page of records. Records show real DEFECT_CODE values.
  5. Take a screenshot post-ingest. Post it to chat for review.

  **COMMIT:** `WP-6 SENTRY ingest: real GCSS-MC export drop-in path with schema sniffing`

  ---

  ### WP-7 — GCSS-MC integration page (Integrations panel)

  **GOAL:** A page where a judge can see the SPIRE → real GCSS-MC field mapping side-by-side. Backed by the dictionary CSVs, so the mapping is verifiable, not just claimed.

  **FILES:**
  - `docs/gcss-mc-schema/` (new directory) — copy the three dictionary CSVs from `tmp/gcss-mc/` into here:
    - `sr_header_dict.csv`
    - `sr_repair_part_dict.csv`
    - `due_in_data_dict.csv`
    - These ARE committed (public schema documentation, not data).
  - `backend/routes/integrations.py` — add `GET /api/integrations/gcss-mc/dictionary` returning a JSON merge of all three dictionaries with SPIRE field annotations layered in.
  - `frontend/src/views/IntegrationsView.tsx` — add a "GCSS-MC Schema Alignment" section. Table format:
    - Column 1: real GCSS-MC field name (from dictionary)
    - Column 2: real type (from dictionary)
    - Column 3: real comments (from dictionary)
    - Column 4: SPIRE field name (or "—" if not mapped)
    - Column 5: SPIRE type / status (`"identical"`, `"hashed for synthetic isomorphism"`, `"not yet ingested"`, etc.)
  - Below the table, a small panel: "Real export ingest: drop a `hashed_header.csv` into SENTRY Upload — schema is validated against this dictionary at parse time."

  **ACCEPTANCE (Playwright):**
  1. Navigate to Integrations view. GCSS-MC Schema Alignment section renders. Table shows ≥30 rows of field mappings with all 5 columns populated. Three dictionary files visibly cited.

  **COMMIT:** `WP-7 Integrations: GCSS-MC schema mapping page with field-by-field dictionary alignment`

  ---

  ### WP-8 — Distribution comparison report

  **GOAL:** A one-page markdown report that shows SPIRE-synthetic vs real-data distributions side by side. Visible in the Admin tab AND committed to the repo.

  **FILES:**
  - New: `docs/gcss-mc-schema/SCHEMA_FIDELITY.md` — generated by a script (`scripts/compare_distributions.py`) that:
    1. Profiles the regenerated synthetic dataset.
    2. Reads the real-data profile from `tmp/gcss-mc/PROFILE.md` (or re-profiles the real data if needed).
    3. Renders a markdown table per dimension: defect-code top 10 (real % vs synth %), priority distribution, echelon distribution, DIC distribution.
    4. Computes a fidelity score per dimension (1 - sum of |real-synth| pp / 2).
  - `frontend/src/views/admin/SchemaFidelityTab.tsx` (new tab in Admin view) — renders the markdown report. Behind security_manager role.

  **ACCEPTANCE:**
  1. The report shows synth fidelity ≥ 0.85 on every dimension.
  2. The Admin tab renders the report cleanly.

  **COMMIT:** `WP-8 Schema fidelity report: synth vs real distribution parity, per-dimension scoring`

  ---

  ### WP-9 — End-to-end stage rehearsal

  **GOAL:** Run the demo arc with the schema-aligned data and the real-export drop-in, verify timing.

  **STEPS:**
  1. Reset to clean demo state.
  2. Boot the dev server.
  3. Run a Playwright script:
     - Sign in as Hayes.
     - Navigate to SENTRY Upload.
     - Click "Load real GCSS-MC export".
     - Upload `tmp/gcss-mc/hashed_header.csv`.
     - Wait for processing to begin.
     - Time elapsed at ingest banner appearance: target < 5 seconds.
     - Switch to Processing tab. Observe ≥ 100 records processed in 30 seconds.
     - Switch to Review Queue. Confirm records visible.
     - Navigate to Integrations. Confirm GCSS-MC Schema Alignment section visible.
     - Navigate to Admin → Schema Fidelity. Confirm fidelity scores ≥ 0.85.
  4. Output a timing report.

  **ACCEPTANCE:**
  1. End-to-end script runs clean. Total time for the schema-credibility beat: < 90 seconds.
  2. Three consecutive runs without flakes.

  **COMMIT:** `WP-9 Stage rehearsal: end-to-end real-data ingest validated by Playwright`

  ---

  ## Final checklist before opening the PR

  1. `git ls-files | xargs grep -lE 'HawkStack|SGDR|1\\.77 ?M|0\\.5795|82\\.95|82\\.12|WEM-Inverted|FCOS|ConvNeXt|five-pillar|spillage scanner|Argus spillage|Tribunal cross-check|Hallucination
  probe|Action Gate'` returns clean (or only sanctioned LICENSE/README/CONTRIBUTING/PR-template hits — list those for the user).
  2. `cd frontend && npx tsc --noEmit` exits 0.
  3. `cd frontend && npm run build` succeeds.
  4. `python -m pytest backend/tests/` passes.
  5. Synthetic dataset regen with seed 42 produces stable fingerprint (compare to pre-change). If fingerprint changes, that's expected (vocab is different) — but document the new fingerprint in
  `GCSS_ALIGNMENT_PLAN.md` with a note that it's the new baseline.
  6. WP-9 Playwright script runs three times clean.
  7. `tmp/` confirmed in `.gitignore`. `git ls-files | grep '^tmp/'` is empty.
  8. `git ls-files | grep '^docs/gcss-mc-schema/'` shows the three dictionary CSVs (committed).
  9. Update `GCSS_ALIGNMENT_PLAN.md` with all checkboxes ticked + a "READY FOR REVIEW" note.

  ## Open the PR

  ```
  gh pr create \
    --base master \
    --head gcss-mc-schema-alignment \
    --title "GCSS-MC schema alignment: synthetic vocabulary + real-export ingest path" \
    --body "$(cat <<'EOF'
  ## Summary
  SPIRE's synthetic dataset and ingest pipeline now match the real GCSS-MC schema. A real sanitized export (103,687 SRs) drops directly into SENTRY Upload, parses against the published GCSS-MC
  dictionary, and feeds the existing classification pipeline without code changes.

  ## What changed
  - Defect-code vocabulary aligned to real GCSS-MC distribution (FCON.CBB / WPNS.CBB dominant) — WP-1
  - Priority codes use real NN A/B/C-Description format with real distribution — WP-2
  - ECHELON_OF_MAINT is numeric 1-4; SERVICE_REQUEST_TYPE field added — WP-3
  - DIC tagging on due-in records — WP-4
  - Three new export endpoints emit data in the exact real schema — WP-5
  - SENTRY Upload tab accepts real GCSS-MC exports via schema-sniffing adapter — WP-6
  - New Integrations panel with field-by-field dictionary alignment — WP-7
  - Schema fidelity report committed and rendered in Admin — WP-8
  - Stage rehearsal verified end-to-end via Playwright — WP-9

  ## Rubric defense
  - **Mission Impact (30%)**: SPIRE is no longer a synthetic toy — it ingests real GCSS-MC schema unmodified.
  - **Technical Innovation (25%)**: schema-aligned synthetic generator + drop-in real-export adapter.
  - **Usability & Design (20%)**: one-click "Load real GCSS-MC export" affordance in SENTRY Upload.
  - **Security & Sustainability (15%)**: real data is sanitized; integration page documents the contract.
  - **Team Collaboration (10%)**: schema work and stage-pivot work are independent — can run in parallel.

  ## Verification
  - WP-9 stage rehearsal: < 90s for the credibility beat, three clean runs.
  - Disclosure scan clean.
  - Fingerprint stable (new baseline documented in GCSS_ALIGNMENT_PLAN.md).
  - All exports validated against real schema dictionaries.

  ## Test plan
  - [ ] Reviewer pulls the branch, drops `hashed_header.csv` into SENTRY Upload, watches it process.
  - [ ] Reviewer compares synth vs real distributions in the Admin → Schema Fidelity tab.
  - [ ] Reviewer confirms the Integrations page documents every real GCSS-MC column.
  - [ ] Reviewer confirms `tmp/gcss-mc/` files are NOT in the diff.
  EOF
  )"
  ```

  **Report back to me with the PR URL, the WP-8 fidelity scores, the WP-9 timing report, and any blockers. Do not merge.**

  ---

  ## Edge cases

  - If the real CSV files have encoding issues (BOM, CRLF, UTF-16): handle them in the WP-6 adapter, don't mutate the source files.
  - If `pytest backend/tests/` fails because a test asserts the old defect-code format: update the test to assert the new format. This is expected; the test should follow the production change.
  - If the synthetic dataset regen takes > 90 seconds, that's fine. Note the runtime in the plan file.
  - If you find a real-data quirk that doesn't match the dictionary (e.g., `MASTER_PRIORITY_CODE` value `"02-DEC-22"` showing up in real data — that's clearly a parser bug in the real export): handle it
  gracefully but flag in `GCSS_ALIGNMENT_PLAN.md` under "Real-data anomalies." Do not let those pollute synthetic generation.
  - If you run out of context mid-session, your last act is to update `GCSS_ALIGNMENT_PLAN.md` with the exact next step and any in-flight code state, then stop. The user resumes in a fresh chat.

  ## What "done" looks like

  When the user pulls `gcss-mc-schema-alignment` and walks the demo arc, the SENTRY beat now starts with **dropping the real 103K-row GCSS-MC export onto the upload tab**. Banner reads "Real GCSS-MC
  export — 103,687 service requests — schema-validated against published data dictionary." Processing starts. Within 90 seconds the credibility moment lands: SPIRE isn't a hackathon mock, it's a system
  that speaks GCSS-MC's actual language. The Integrations page proves the schema match field-by-field. The Admin Schema Fidelity tab shows synth ≥ 0.85 fidelity per dimension.

  Begin.
