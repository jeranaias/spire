# Exact-pinned for reproducible builds (800-171 3.4.1). Bump via Dependabot.
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.4
cryptography==46.0.7
httpx==0.28.1
python-multipart==0.0.26
defusedxml==0.7.1  # hardened XML parsing (XXE / entity-expansion defense)
# torch + opencv-python-headless are needed ONLY for the live
# ThermalHawk inference path. They live in requirements-ml.txt so the
# default Fly deploy stays small (~150MB image) and cold starts fast.
# Install them on the deploy box that needs the live feed:
#   pip install -r backend/requirements-ml.txt
# model_hooks degrades gracefully when torch isn't importable.
