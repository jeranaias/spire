fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
cryptography>=43.0.0
httpx>=0.27.0
python-multipart>=0.0.20
# Live model inference — ThermalHawk (Thornveil-licensed, val mAP (accuracy)
# on Anti-UAV410) loads when SPIRE_THERMALHAWK_WEIGHTS points at the
# checkpoint. torch + opencv-python-headless are required for the
# forward pass and thermal frame preprocessing.
torch>=2.0
opencv-python-headless>=4.8
numpy>=1.26
