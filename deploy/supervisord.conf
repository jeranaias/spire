; SPIRE — supervisord program list for the combined image.
; Runs nginx (foreground) and uvicorn (foreground) under one PID 1.
; If either dies, the container exits and the orchestrator restarts it.

[program:uvicorn]
command=/usr/local/bin/python -m uvicorn backend.main:app --host 127.0.0.1 --port 8700 --proxy-headers --forwarded-allow-ips=*
directory=/opt/spire
autostart=true
autorestart=true
priority=10
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

[program:nginx]
command=/usr/sbin/nginx -g "daemon off;"
autostart=true
autorestart=true
priority=20
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
