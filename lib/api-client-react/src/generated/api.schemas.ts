/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Marine Corps logistics calculator API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export type Echelon = (typeof Echelon)[keyof typeof Echelon];

export const Echelon = {
  fireteam: "fireteam",
  squad: "squad",
  section: "section",
  platoon: "platoon",
  company: "company",
  battalion: "battalion",
  regiment: "regiment",
  battery: "battery",
  team: "team",
} as const;

/**
 * Relationship of the unit to the regiment (organic subordinate, attached, or in direct support)
 */
export type UnitRole = (typeof UnitRole)[keyof typeof UnitRole];

export const UnitRole = {
  organic: "organic",
  attached: "attached",
  in_support: "in_support",
} as const;

/**
 * Doctrinal ammo planning posture. combat_load = initial issue quantity; assault = daily assault rate; sustain = daily sustain rate.
 */
export type AmmoPosture = (typeof AmmoPosture)[keyof typeof AmmoPosture];

export const AmmoPosture = {
  combat_load: "combat_load",
  assault: "assault",
  sustain: "sustain",
} as const;

export interface WeaponDodicRate {
  /** Stable identifier for this weapon-DODIC rate row */
  id: string;
  dodic: string;
  nomenclature: string;
  catalogItemId: string;
  /** @minimum 0 */
  gceCombatLoad: number;
  /** @minimum 0 */
  gceAssaultRate: number;
  /** @minimum 0 */
  gceSustainRate: number;
  /** @minimum 0 */
  nonGceCombatLoad: number;
  /** @minimum 0 */
  nonGceAssaultRate: number;
  /** @minimum 0 */
  nonGceSustainRate: number;
}

export interface WeaponSystem {
  id: string;
  tamcn?: string | null;
  name: string;
  /** Whether this weapon system is a GCE (Ground Combat Element) system */
  isGce: boolean;
  notes?: string | null;
  /** DODIC codes and rates for this weapon system */
  dodics: WeaponDodicRate[];
}

/**
 * Editable doctrinal rates for a weapon-DODIC pair. All fields optional; only provided fields are updated.
 */
export interface WeaponDodicRateInput {
  /** @minimum 0 */
  gceCombatLoad?: number;
  /** @minimum 0 */
  gceAssaultRate?: number;
  /** @minimum 0 */
  gceSustainRate?: number;
  /** @minimum 0 */
  nonGceCombatLoad?: number;
  /** @minimum 0 */
  nonGceAssaultRate?: number;
  /** @minimum 0 */
  nonGceSustainRate?: number;
}

export interface UnitWeapon {
  id: string;
  unitId: string;
  weaponSystemId: string;
  weaponName: string;
  tamcn?: string | null;
  /** @minimum 1 */
  quantity: number;
  isGce: boolean;
}

export interface UnitWeaponInput {
  weaponSystemId: string;
  /** @minimum 1 */
  quantity: number;
}

export interface UnitWeaponUpdate {
  /** @minimum 1 */
  quantity: number;
}

export type Climate = (typeof Climate)[keyof typeof Climate];

export const Climate = {
  temperate: "temperate",
  arid: "arid",
  tropical: "tropical",
  arctic: "arctic",
} as const;

export type OpTempo = (typeof OpTempo)[keyof typeof OpTempo];

export const OpTempo = {
  garrison: "garrison",
  sustained: "sustained",
  high: "high",
  combat: "combat",
} as const;

/**
 * Marine Corps Class of Supply
 */
export type SupplyClass = (typeof SupplyClass)[keyof typeof SupplyClass];

export const SupplyClass = {
  I: "I",
  III: "III",
  V: "V",
  VIII: "VIII",
  IX: "IX",
} as const;

export interface Unit {
  id: string;
  name: string;
  callsign?: string | null;
  echelon: Echelon;
  /** @minimum 1 */
  personnel: number;
  commander?: string | null;
  location?: string | null;
  climate: Climate;
  opTempo: OpTempo;
  /**
   * Planned mission duration in days
   * @minimum 1
   */
  missionDays: number;
  /**
   * Computed readiness percentage (0-100)
   * @minimum 0
   * @maximum 100
   */
  readiness: number;
  /** @minimum 0 */
  deficiencyCount: number;
  role: UnitRole;
  ammoPosture: AmmoPosture;
  /** True if this unit is a GCE (Ground Combat Element); affects which doctrinal ammo rates apply */
  isGce: boolean;
  /** Distribution list of email addresses pre-filled when the unit's schedules are emailed (S-4, supporting battalion, logistics POCs, etc.) */
  distroEmails: string[];
  /** Optional CC distribution list pre-filled into the cc= field of the schedule email — used for visibility recipients (supporting battalion logistics, company commander, higher HQ) who are not action addressees. */
  distroCcEmails: string[];
  /** Optional BCC distribution list pre-filled into the bcc= field of the schedule email — used for silent recipients (operations watch officer, archive mailbox, COC liaison) who should not be visible to To or CC recipients. */
  distroBccEmails: string[];
  createdAt: string;
  updatedAt: string;
}

export interface UnitInput {
  /** @minLength 1 */
  name: string;
  callsign?: string | null;
  echelon: Echelon;
  /** @minimum 1 */
  personnel: number;
  commander?: string | null;
  location?: string | null;
  climate: Climate;
  opTempo: OpTempo;
  /** @minimum 1 */
  missionDays: number;
  role?: UnitRole;
  ammoPosture?: AmmoPosture;
  isGce?: boolean;
  /** Distribution list of email addresses pre-filled when the unit's schedules are emailed */
  distroEmails?: string[];
  /** Optional CC distribution list pre-filled into the cc= field of the schedule email */
  distroCcEmails?: string[];
  /** Optional BCC distribution list pre-filled into the bcc= field of the schedule email — silent recipients (operations watch officer, archive mailbox, COC liaison) hidden from To and CC recipients */
  distroBccEmails?: string[];
}

export type CatalogItemCriticality =
  (typeof CatalogItemCriticality)[keyof typeof CatalogItemCriticality];

export const CatalogItemCriticality = {
  low: "low",
  medium: "medium",
  high: "high",
  critical: "critical",
} as const;

/**
 * Minimal unit reference for catalog item usage lists
 */
export interface CatalogItemUsageUnit {
  id: string;
  name: string;
}

export interface CatalogItem {
  id: string;
  supplyClass: SupplyClass;
  name: string;
  /** National Stock Number */
  nsn?: string | null;
  /** Unit of issue (e.g. "MRE", "gal", "battery", "round", "L") */
  unit: string;
  /** Base consumption per Marine per day at temperate / sustained tempo */
  baseDailyRate: number;
  criticality: CatalogItemCriticality;
  notes?: string | null;
  /** True if this item was added manually by a planner (not from the standard catalog) */
  isCustom: boolean;
  /** When this catalog item was first created */
  createdAt: string;
  /** When this catalog item's details were last edited (equal to createdAt if never edited) */
  updatedAt: string;
  /**
   * Number of units that currently track this catalog item on their supply list. Populated by listCatalogItems; may be omitted on other endpoints.
   * @minimum 0
   */
  usedByUnitCount?: number;
  /** Units that currently track this catalog item, sorted by unit name. Populated by listCatalogItems; may be omitted on other endpoints. */
  usedByUnits?: CatalogItemUsageUnit[];
}

export type CatalogItemUpdateInputCriticality =
  (typeof CatalogItemUpdateInputCriticality)[keyof typeof CatalogItemUpdateInputCriticality];

export const CatalogItemUpdateInputCriticality = {
  low: "low",
  medium: "medium",
  high: "high",
  critical: "critical",
} as const;

/**
 * Editable fields on a global custom catalog item
 */
export interface CatalogItemUpdateInput {
  /** @minLength 1 */
  name: string;
  supplyClass?: SupplyClass;
  /**
   * Unit of issue (e.g. "ea", "gal", "round", "case")
   * @minLength 1
   */
  unit?: string;
  /** National Stock Number / DODIC. Send null to clear. */
  nsn?: string | null;
  notes?: string | null;
  criticality?: CatalogItemUpdateInputCriticality;
  /**
   * Base consumption per Marine per day. Set to 0 to make the item reference-only.
   * @minimum 0
   */
  baseDailyRate?: number;
}

export interface DeleteCatalogItemResult {
  deletedItemId: string;
  /** Number of units that had this item on their supply list */
  affectedUnits: number;
  /** Number of unit supply entries cascaded by the delete */
  removedSupplyEntries: number;
  /** Number of future resupply events cancelled by the delete */
  cancelledResupplyEvents: number;
  /** Pass to POST /catalog/items/{itemId}/restore to undo the delete within the grace window. Equal to deletedItemId. */
  restoreToken: string;
  /** ISO timestamp after which the restore endpoint will return 410 Gone for this item. */
  restoreExpiresAt: string;
}

export interface RestoreCatalogItemResult {
  restoredItem: CatalogItem;
  /** Number of unit supply entries re-created from the snapshot */
  restoredSupplyEntries: number;
  /** Number of future resupply events re-created from the snapshot */
  restoredResupplyEvents: number;
}

export interface DeleteUnitSupplyResult {
  unitId: string;
  removedItemId: string;
  /** Name of the item at the time of removal, for use in the undo toast */
  removedItemName: string;
  /** On-hand count at the time of removal */
  hadOnHand: number;
  /** Number of future resupply events cancelled by the delete */
  cancelledResupplyEvents: number;
  /** True if a unit-scoped custom catalog item was deleted alongside the supply entry */
  removedCatalogItem: boolean;
  /** ISO timestamp after which POST /units/{unitId}/supply/{itemId}/restore will return 410 Gone for this entry. */
  restoreExpiresAt: string;
}

export type SupplyEntryStatus =
  (typeof SupplyEntryStatus)[keyof typeof SupplyEntryStatus];

export const SupplyEntryStatus = {
  green: "green",
  amber: "amber",
  red: "red",
} as const;

export interface SupplyEntry {
  id: string;
  unitId: string;
  itemId: string;
  item: CatalogItem;
  /** @minimum 0 */
  onHand: number;
  /** Adjusted daily consumption for this unit (climate / tempo / personnel) */
  dailyConsumption: number;
  /** Computed days of supply remaining */
  daysOfSupply: number;
  /** Effective required quantity (requiredOverride when set, otherwise auto-computed) */
  required: number;
  /** Required minus on-hand (>= 0) */
  shortfall: number;
  status: SupplyEntryStatus;
  /** Planner-set required quantity for this item on this unit. Null means use the auto-computed value. Zero means not a requirement. */
  requiredOverride?: number | null;
  /** False when requiredOverride is exactly 0 — item is tracked but not counted toward readiness or DOS. */
  isRequirement: boolean;
  /** Human-readable description of how the burn rate was computed (weapon-driven for Class V) */
  burnBreakdown?: string | null;
  /** Total combat load target quantity (weapon-driven Class V only) */
  combatLoadTarget?: number | null;
  updatedAt: string;
}

export interface RestoreUnitSupplyResult {
  restoredEntry: SupplyEntry;
  /** Number of future resupply events re-created from the snapshot */
  restoredResupplyEvents: number;
  /** True if a unit-scoped custom catalog item was also re-created */
  restoredCatalogItem: boolean;
}

/**
 * Optional criticality level; defaults to "low" when omitted
 */
export type CustomItemInputCriticality =
  | (typeof CustomItemInputCriticality)[keyof typeof CustomItemInputCriticality]
  | null;

export const CustomItemInputCriticality = {
  low: "low",
  medium: "medium",
  high: "high",
  critical: "critical",
} as const;

/**
 * Payload for manually adding a non-catalog item to a unit
 */
export interface CustomItemInput {
  /**
   * Item name
   * @minLength 1
   */
  name: string;
  supplyClass: SupplyClass;
  /**
   * Unit of issue (e.g. "ea", "gal", "round", "case")
   * @minLength 1
   */
  unit: string;
  /** Optional NSN or DODIC */
  nsn?: string | null;
  /**
   * Optional base daily consumption per Marine per day. Must be >= 0. Omit or set to null for reference-only tracking (no DOS computed).
   * @minimum 0
   */
  baseDailyRate?: number | null;
  /** Optional criticality level; defaults to "low" when omitted */
  criticality?: CustomItemInputCriticality;
  notes?: string | null;
}

/**
 * Criticality level
 */
export type UpdateCustomItemInputCriticality =
  (typeof UpdateCustomItemInputCriticality)[keyof typeof UpdateCustomItemInputCriticality];

export const UpdateCustomItemInputCriticality = {
  low: "low",
  medium: "medium",
  high: "high",
  critical: "critical",
} as const;

/**
 * Payload for editing an existing custom (unit-scoped) catalog item. Only allowed for items where isCustom is true and the item is scoped to the unit.
 */
export interface UpdateCustomItemInput {
  /**
   * Item name
   * @minLength 1
   */
  name: string;
  supplyClass: SupplyClass;
  /**
   * Unit of issue (e.g. "ea", "gal", "round", "case")
   * @minLength 1
   */
  unit: string;
  /** Optional NSN or DODIC */
  nsn?: string | null;
  /**
   * Optional base daily consumption per Marine per day. Must be >= 0. Set to null for reference-only tracking (no DOS computed).
   * @minimum 0
   */
  baseDailyRate?: number | null;
  /** Criticality level */
  criticality: UpdateCustomItemInputCriticality;
  notes?: string | null;
}

/**
 * In-place update for an existing supply row. Provide at least one of onHand or requiredOverride. Omitted fields are left unchanged.
 */
export interface SupplyEntryUpdate {
  /**
   * New on-hand quantity. Omit to leave the current value unchanged.
   * @minimum 0
   */
  onHand?: number;
  /**
   * Planner-set required quantity. Null clears any existing override (returns to auto-computed). Zero means "not a requirement for this unit". Omit to leave the current override unchanged.
   * @minimum 0
   */
  requiredOverride?: number | null;
}

/**
 * Either provide itemId (catalog pick) or customItem (manual entry). Exactly one must be present.
 */
export interface SupplyEntryInput {
  /** Existing catalog item ID. Omit when adding a custom item. */
  itemId?: string;
  /** @minimum 0 */
  onHand: number;
  /**
   * Optional planner-set required quantity. Null clears any existing override (returns to auto-computed). Zero means "not a requirement for this unit".
   * @minimum 0
   */
  requiredOverride?: number | null;
  customItem?: CustomItemInput;
  /** When adding a custom item, also save it as a global catalog entry visible to all units. */
  saveToCatalog?: boolean;
}

export interface CopyFromUnitInput {
  /** The ID of the unit to copy supply items from */
  sourceUnitId: string;
}

export interface CopyFromUnitResult {
  /**
   * Number of new items added from the source unit
   * @minimum 0
   */
  added: number;
  /**
   * Number of items already tracked on this unit (not duplicated)
   * @minimum 0
   */
  skipped: number;
}

export type ClassSummaryStatus =
  (typeof ClassSummaryStatus)[keyof typeof ClassSummaryStatus];

export const ClassSummaryStatus = {
  green: "green",
  amber: "amber",
  red: "red",
} as const;

export interface ClassSummary {
  supplyClass: SupplyClass;
  label: string;
  itemCount: number;
  worstDaysOfSupply: number;
  status: ClassSummaryStatus;
}

export type ResupplyEventStatus =
  (typeof ResupplyEventStatus)[keyof typeof ResupplyEventStatus];

export const ResupplyEventStatus = {
  projected: "projected",
  planned: "planned",
  in_transit: "in_transit",
  delivered: "delivered",
  cancelled: "cancelled",
} as const;

export interface ResupplyEvent {
  id: string;
  unitId: string;
  unitName: string;
  supplyClass: SupplyClass;
  itemId?: string | null;
  itemName?: string | null;
  quantity: number;
  unit: string;
  scheduledFor: string;
  status: ResupplyEventStatus;
  assignedTo?: string | null;
  notes?: string | null;
  createdAt: string;
}

export interface UnitDetail {
  unit: Unit;
  supplyByClass: ClassSummary[];
  entries: SupplyEntry[];
  upcomingResupply: ResupplyEvent[];
  weapons: UnitWeapon[];
}

export interface CalculationRequest {
  /** @minimum 1 */
  days: number;
  /**
   * Override personnel count for what-if calc
   * @minimum 1
   */
  personnel?: number | null;
  climate: Climate;
  opTempo: OpTempo;
}

export interface CalculationLine {
  item: CatalogItem;
  dailyConsumption: number;
  totalRequired: number;
}

export interface CalculationResult {
  days: number;
  personnel: number;
  climate: Climate;
  opTempo: OpTempo;
  lines: CalculationLine[];
}

export interface ResupplyEventInput {
  supplyClass: SupplyClass;
  itemId?: string | null;
  /** @minimum 0 */
  quantity: number;
  unit: string;
  scheduledFor: string;
  assignedTo?: string | null;
  notes?: string | null;
}

export type ResupplyEventStatusInputStatus =
  (typeof ResupplyEventStatusInputStatus)[keyof typeof ResupplyEventStatusInputStatus];

export const ResupplyEventStatusInputStatus = {
  projected: "projected",
  planned: "planned",
  in_transit: "in_transit",
  delivered: "delivered",
  cancelled: "cancelled",
} as const;

export interface ResupplyEventStatusInput {
  status: ResupplyEventStatusInputStatus;
  assignedTo?: string | null;
}

export interface ClassBreakdown {
  supplyClass: SupplyClass;
  label: string;
  green: number;
  amber: number;
  red: number;
}

export interface DashboardSummary {
  unitCount: number;
  personnelCount: number;
  readinessAvg: number;
  deficiencyCount: number;
  criticalDeficiencyCount: number;
  upcomingResupplyCount: number;
  classBreakdown: ClassBreakdown[];
}

export type ClassDetailTotals = {
  green: number;
  amber: number;
  red: number;
  unitsTracking: number;
  unitsAtRisk: number;
};

export type ClassUnitStatusWorstStatus =
  (typeof ClassUnitStatusWorstStatus)[keyof typeof ClassUnitStatusWorstStatus];

export const ClassUnitStatusWorstStatus = {
  green: "green",
  amber: "amber",
  red: "red",
} as const;

export interface ClassUnitStatus {
  unitId: string;
  unitName: string;
  callsign?: string | null;
  echelon: Echelon;
  personnel: number;
  location?: string | null;
  itemCount: number;
  greenCount: number;
  amberCount: number;
  redCount: number;
  worstStatus: ClassUnitStatusWorstStatus;
  worstDaysOfSupply: number;
}

export type ClassItemEntryStatus =
  (typeof ClassItemEntryStatus)[keyof typeof ClassItemEntryStatus];

export const ClassItemEntryStatus = {
  green: "green",
  amber: "amber",
  red: "red",
} as const;

export interface ClassItemEntry {
  entryId: string;
  unitId: string;
  unitName: string;
  itemId: string;
  itemName: string;
  onHand: number;
  required: number;
  shortfall: number;
  unit: string;
  daysOfSupply: number;
  status: ClassItemEntryStatus;
  criticality: string;
  burnBreakdown?: string | null;
  combatLoadTarget?: number | null;
  ammoPosture?: string | null;
}

export interface ClassDetail {
  supplyClass: SupplyClass;
  label: string;
  totals: ClassDetailTotals;
  units: ClassUnitStatus[];
  items: ClassItemEntry[];
}

export type DodicBreakdownTotals = {
  /** Number of distinct DODICs tracked across the regiment */
  dodicCount: number;
  /** Sum of on-hand rounds across every DODIC */
  totalOnHand: number;
  /** Sum of daily required rounds across every DODIC */
  totalDailyRequired: number;
  /** Sum of mission shortfall rounds across every DODIC */
  totalShortfall: number;
};

export type DodicBreakdownItemStatus =
  (typeof DodicBreakdownItemStatus)[keyof typeof DodicBreakdownItemStatus];

export const DodicBreakdownItemStatus = {
  green: "green",
  amber: "amber",
  red: "red",
} as const;

export type DodicContributingUnitStatus =
  (typeof DodicContributingUnitStatus)[keyof typeof DodicContributingUnitStatus];

export const DodicContributingUnitStatus = {
  green: "green",
  amber: "amber",
  red: "red",
} as const;

export interface DodicContributingUnit {
  unitId: string;
  unitName: string;
  callsign?: string | null;
  echelon: Echelon;
  onHand: number;
  dailyRequired: number;
  required: number;
  shortfall: number;
  daysOfSupply: number;
  status: DodicContributingUnitStatus;
}

export interface DodicBreakdownItem {
  /** DODIC product code (e.g. A059). Empty string if no weapon mapping exists. */
  dodic: string;
  catalogItemId: string;
  /** Catalog item name used as the DODIC nomenclature */
  nomenclature: string;
  /** Unit of issue (e.g. "rd", "ea") */
  unit: string;
  totalOnHand: number;
  totalDailyRequired: number;
  /** Total rounds required across mission days for all contributing units */
  totalRequired: number;
  totalShortfall: number;
  /** Aggregate DOS = totalOnHand / totalDailyRequired. 999 when daily required is zero. */
  aggregateDaysOfSupply: number;
  status: DodicBreakdownItemStatus;
  /** Count of units carrying or burning this DODIC */
  unitsTracking: number;
  /** Count of units with a mission shortfall on this DODIC */
  unitsShort: number;
  contributingUnits: DodicContributingUnit[];
}

export interface DodicBreakdown {
  totals: DodicBreakdownTotals;
  items: DodicBreakdownItem[];
}

export type DeficiencySeverity =
  (typeof DeficiencySeverity)[keyof typeof DeficiencySeverity];

export const DeficiencySeverity = {
  amber: "amber",
  red: "red",
} as const;

export interface Deficiency {
  id: string;
  unitId: string;
  unitName: string;
  supplyClass: SupplyClass;
  itemName: string;
  daysOfSupply: number;
  shortfall: number;
  unit: string;
  severity: DeficiencySeverity;
  flaggedAt: string;
}

export interface ForecastEntry {
  unitId: string;
  unitName: string;
  supplyClass: SupplyClass;
  itemName: string;
  projectedStockoutDate: string;
  daysUntilStockout: number;
  recommendedQuantity: number;
  unit: string;
}

/**
 * Which distribution-list bucket the malformed value lives in
 */
export type DistroAuditEntryBucket =
  (typeof DistroAuditEntryBucket)[keyof typeof DistroAuditEntryBucket];

export const DistroAuditEntryBucket = {
  to: "to",
  cc: "cc",
  bcc: "bcc",
} as const;

export interface DistroAuditEntry {
  /** Which distribution-list bucket the malformed value lives in */
  bucket: DistroAuditEntryBucket;
  /** The literal stored value that failed the email shape regex */
  value: string;
}

export interface DistroAuditUnit {
  unitId: string;
  unitName: string;
  echelon: Echelon;
  callsign: string | null;
  invalidEntries: DistroAuditEntry[];
  /**
   * Total number of malformed entries across all buckets for this unit
   * @minimum 1
   */
  invalidCount: number;
}

/**
 * `sent` — digest was delivered to SMTP. `skipped_no_flags` — audit found nothing to report so no email was sent. `skipped_no_recipients` — flagged units exist but `COMMS_HYGIENE_TO` is empty. `skipped_no_smtp` — SMTP not configured; digest preview was logged instead. `failed` — SMTP rejected the send (see `errorMessage`).
 */
export type CommsHygieneOutcome =
  (typeof CommsHygieneOutcome)[keyof typeof CommsHygieneOutcome];

export const CommsHygieneOutcome = {
  sent: "sent",
  skipped_no_flags: "skipped_no_flags",
  skipped_no_recipients: "skipped_no_recipients",
  skipped_no_smtp: "skipped_no_smtp",
  failed: "failed",
} as const;

export interface CommsHygieneRun {
  id: string;
  ranAt: string;
  /**
   * Number of units scanned during this run
   * @minimum 0
   */
  auditedCount: number;
  /** @minimum 0 */
  flaggedCount: number;
  /** @minimum 0 */
  invalidCount: number;
  /** Recipient list the digest was (or would have been) sent to */
  recipients: string[];
  cc: string[];
  outcome: CommsHygieneOutcome;
  /** SMTP / runtime error message when `outcome` is `failed` */
  errorMessage: string | null;
}

export interface DistroAudit {
  /**
   * Total number of units inspected
   * @minimum 0
   */
  scannedUnitCount: number;
  /** @minimum 0 */
  flaggedUnitCount: number;
  /**
   * Total number of malformed entries across all flagged units
   * @minimum 0
   */
  invalidEntryCount: number;
  units: DistroAuditUnit[];
  /** The most recent comms-hygiene digest run, regardless of outcome. `null` until the scheduler has run at least once. Use `outcome` to distinguish a successful send from a skipped/failed attempt. */
  lastRun: CommsHygieneRun | null;
  /** The most recent run whose outcome was `sent`, or `null` if no digest has ever been delivered. Surfaced separately so planners can see when the last digest actually went out even after a recent failure. */
  lastSuccessfulSend: CommsHygieneRun | null;
}

/**
 * Run-history stats for the dashboard footnote. Lets planners see how many digest runs are stored and when older ones will fall off under the active retention policy.
 */
export interface CommsHygieneStats {
  /**
   * Total number of `comms_hygiene_runs` rows currently stored.
   * @minimum 0
   */
  totalRuns: number;
  /** Timestamp of the oldest stored run, or `null` when the table is empty. */
  oldestRanAt: string | null;
  /**
   * Effective retention horizon in days. Equal to `retentionDaysOverride` when an in-app override is active, otherwise `retentionDaysDefault`. 0 means retention is disabled.
   * @minimum 0
   */
  retentionDays: number;
  /**
   * Dashboard-set retention override in days, or `null` when no override is active and the env-var default applies. Surfaced so the footnote can flag when an admin has temporarily widened (or disabled) retention.
   * @minimum 0
   */
  retentionDaysOverride: number | null;
  /**
   * Env-var (`COMMS_HYGIENE_RETENTION_DAYS`, default 180) used when no override is active. Returned alongside the effective value so the dashboard can label the source.
   * @minimum 0
   */
  retentionDaysDefault: number;
  /** When the oldest stored run is expected to age out of the table (oldest `ranAt` plus `retentionDays`). `null` when the table is empty or retention is disabled. */
  oldestExpiresAt: string | null;
  /**
   * Configured "expires soon" window in days (`COMMS_HYGIENE_NEAR_EXPIRY_WINDOW_DAYS`, default 7). Rows whose projected expiry falls within this window of "now" are considered near-expiry. Always 0 when retention is disabled.
   * @minimum 0
   */
  nearExpiryWindowDays: number;
  /**
   * Number of stored runs whose projected expiry (`ranAt` plus `retentionDays`) is within `nearExpiryWindowDays` of the current time, including any rows that have already passed their expiry but have not yet been pruned. Always 0 when retention is disabled.
   * @minimum 0
   */
  nearExpiryCount: number;
}

/**
 * Effective comms-hygiene retention settings. The dashboard reads this to render the in-app retention control (current value, the env-var fallback, and the last time the override was changed).
 */
export interface CommsHygieneSettings {
  /**
   * Effective retention horizon in days that the prune sweep and stats endpoint use right now. 0 means retention is disabled.
   * @minimum 0
   */
  retentionDays: number;
  /**
   * Dashboard-set override; `null` when no override is active and the env-var default applies.
   * @minimum 0
   */
  retentionDaysOverride: number | null;
  /**
   * Env-var (`COMMS_HYGIENE_RETENTION_DAYS`) value used when no override is set. Lets the UI label the fallback alongside any active override.
   * @minimum 0
   */
  retentionDaysDefault: number;
  /** Last time the settings row was written, or `null` if the override has never been touched. */
  updatedAt: string | null;
}

/**
 * Payload for `PUT /dashboard/comms-hygiene-settings`. Set `retentionDaysOverride` to a non-negative integer (0 disables pruning entirely) to install an override, or `null` to clear it and fall back to the env-var default.
 */
export interface CommsHygieneSettingsUpdate {
  /**
   * New override in days, or `null` to clear the override. Capped at 3650 days (~10 years) to keep the prune cutoff math sane.
   * @minimum 0
   * @maximum 3650
   */
  retentionDaysOverride: number | null;
}

/**
 * When `emailSent` is false, why the email was not sent. `no_flagged_entries` means the digest was suppressed because every distro list is clean. `no_recipients` and `smtp_not_configured` indicate a server-side configuration gap.
 */
export type DistroAuditDigestResultReason =
  (typeof DistroAuditDigestResultReason)[keyof typeof DistroAuditDigestResultReason];

export const DistroAuditDigestResultReason = {
  no_flagged_entries: "no_flagged_entries",
  no_recipients: "no_recipients",
  smtp_not_configured: "smtp_not_configured",
} as const;

/**
 * Outcome of an on-demand `runCommsHygieneOnce` invocation.
 */
export interface DistroAuditDigestResult {
  /**
   * Number of units inspected during the run
   * @minimum 0
   */
  audited: number;
  /**
   * Number of units with at least one malformed entry
   * @minimum 0
   */
  flagged: number;
  /**
   * Total number of malformed entries across all flagged units
   * @minimum 0
   */
  invalid: number;
  /** True when an email was actually dispatched to recipients */
  emailSent: boolean;
  /** When `emailSent` is false, why the email was not sent. `no_flagged_entries` means the digest was suppressed because every distro list is clean. `no_recipients` and `smtp_not_configured` indicate a server-side configuration gap. */
  reason?: DistroAuditDigestResultReason;
}

/**
 * Rendered digest payload (subject / text / html) that the live send path would dispatch, returned without invoking SMTP.
 */
export interface DistroAuditDigestPreview {
  /**
   * Number of units inspected during the preview build
   * @minimum 0
   */
  audited: number;
  /**
   * Number of units with at least one malformed entry
   * @minimum 0
   */
  flagged: number;
  /**
   * Total number of malformed entries across all flagged units
   * @minimum 0
   */
  invalid: number;
  /** True when no entries were flagged. The live send path suppresses the email in this case; the preview still returns the rendered payload so callers can confirm what would have been produced. */
  suppressed: boolean;
  /** Email subject line that the live send would use */
  subject: string;
  /** Plain-text email body that the live send would use */
  text: string;
  /** HTML email body that the live send would use */
  html: string;
}

export type ActivityEntryKind =
  (typeof ActivityEntryKind)[keyof typeof ActivityEntryKind];

export const ActivityEntryKind = {
  resupply_planned: "resupply_planned",
  resupply_delivered: "resupply_delivered",
  deficiency_flagged: "deficiency_flagged",
  sync_completed: "sync_completed",
  unit_created: "unit_created",
  supply_updated: "supply_updated",
} as const;

export interface ActivityEntry {
  id: string;
  kind: ActivityEntryKind;
  message: string;
  unitId?: string | null;
  unitName?: string | null;
  timestamp: string;
}

export interface SyncStatus {
  connected: boolean;
  upstreamSystem: string;
  lastSyncAt?: string | null;
  pendingChanges: number;
  latencyMs?: number | null;
  autoSyncEnabled: boolean;
  autoSyncIntervalMinutes: number;
}

export interface SyncSettingsInput {
  autoSyncEnabled?: boolean;
  /**
   * @minimum 1
   * @maximum 60
   */
  autoSyncIntervalMinutes?: number;
}

export interface SyncOutboxItem {
  id: string;
  entityKind: string;
  entityId: string;
  unitId?: string | null;
  op: string;
  createdAt: string;
  status: string;
  lastError?: string | null;
  itemName?: string | null;
  unitName?: string | null;
}

export interface SyncPushResult {
  outboxId: string;
  entityKind: string;
  entityId: string;
  success: boolean;
  error?: string | null;
  unitName?: string | null;
  itemName?: string | null;
}

export interface SyncLastRun {
  id: string;
  startedAt: string;
  finishedAt?: string | null;
  pushedCount: number;
  failedCount: number;
  catalogMatched: number;
  catalogNew: number;
  catalogChanged: number;
  latencyMs?: number | null;
  results?: SyncPushResult[];
}

export interface SyncRunSummary {
  id: string;
  startedAt: string;
  finishedAt?: string | null;
  pushedCount: number;
  failedCount: number;
  catalogMatched: number;
  catalogNew: number;
  catalogChanged: number;
  latencyMs?: number | null;
  results?: SyncPushResult[];
}

export interface SupplyHistoryPoint {
  snapshotAt: string;
  onHand: number;
  source: string;
  actorNote?: string | null;
}

export interface SupplyHistoryItem {
  itemId: string;
  itemName: string;
  supplyClass: string;
  unit: string;
  baseDailyRate: number;
  criticality: string;
  series: SupplyHistoryPoint[];
  observedBurnRate?: number | null;
  doctrinaRate: number;
  burnRateDelta?: number | null;
  lastConfirmedAt?: string | null;
}

export interface BaselineSnapshotItem {
  itemId: string;
  itemName: string;
  supplyClass: string;
  unit: string;
  onHand: number;
  dailyConsumption: number;
  daysOfSupply: number;
  status: string;
}

export interface BaselineSnapshotData {
  unitId: string;
  unitName: string;
  personnel: number;
  climate: string;
  opTempo: string;
  missionDays: number;
  items: BaselineSnapshotItem[];
}

export interface SupplyBaseline {
  id: string;
  label: string;
  notes?: string | null;
  unitId?: string | null;
  frozenAt: string;
  createdAt: string;
  snapshotData: BaselineSnapshotData;
}

export interface CreateBaselineInput {
  label: string;
  notes?: string;
}

export type CommsDeniedForecastInputBurnModel =
  (typeof CommsDeniedForecastInputBurnModel)[keyof typeof CommsDeniedForecastInputBurnModel];

export const CommsDeniedForecastInputBurnModel = {
  doctrinal: "doctrinal",
  observed: "observed",
  "worst-of-both": "worst-of-both",
} as const;

export interface CommsDeniedForecastInput {
  baselineId?: string;
  horizonDays?: number;
  burnModel?: CommsDeniedForecastInputBurnModel;
  safetyMarginDays?: number;
  resupplyLeadDays?: number;
}

export interface ForecastLine {
  itemId: string;
  itemName: string;
  supplyClass: string;
  unit: string;
  startingOnHand: number;
  doctrinalDailyRate: number;
  observedDailyRate?: number | null;
  effectiveDailyRate: number;
  projectedDaysUntilStockout: number;
  projectedStockoutDate?: string | null;
  needsResupply: boolean;
  recommendedQuantity: number;
  recommendedDeliveryDate?: string | null;
}

export interface CommsDeniedForecast {
  unitId: string;
  unitName: string;
  baselineId?: string | null;
  baselineLabel?: string | null;
  planningDate: string;
  horizonDays: number;
  burnModel: string;
  safetyMarginDays: number;
  resupplyLeadDays: number;
  lines: ForecastLine[];
}

export interface PublishScheduleLineInput {
  itemId: string;
  itemName: string;
  supplyClass: string;
  unit: string;
  recommendedQuantity: number;
  recommendedDeliveryDate?: string | null;
}

export type PublishScheduleInputBurnModel =
  (typeof PublishScheduleInputBurnModel)[keyof typeof PublishScheduleInputBurnModel];

export const PublishScheduleInputBurnModel = {
  doctrinal: "doctrinal",
  observed: "observed",
  "worst-of-both": "worst-of-both",
} as const;

export interface PublishScheduleInput {
  label: string;
  baselineId?: string;
  horizonDays?: number;
  burnModel?: PublishScheduleInputBurnModel;
  safetyMarginDays?: number;
  resupplyLeadDays?: number;
  lines: PublishScheduleLineInput[];
}

export interface PublishedSchedule {
  id: string;
  label: string;
  unitId: string;
  baselineId?: string | null;
  horizonDays: number;
  burnModel: string;
  safetyMarginDays: number;
  resupplyLeadDays: number;
  publishedAt?: string | null;
  shareToken?: string | null;
  createdAt?: string;
  eventsCreated?: number;
}

export interface ScheduleEventItem {
  id: string;
  supplyClass: string;
  itemId?: string | null;
  itemName?: string | null;
  quantity: number;
  unit: string;
  scheduledFor: string;
  status: string;
  assignedTo?: string | null;
  notes?: string | null;
}

export interface ScheduleDetail {
  id: string;
  label: string;
  unitId: string;
  unitName: string;
  unitEchelon?: string | null;
  unitLocation?: string | null;
  unitClimate?: string | null;
  unitOpTempo?: string | null;
  unitAmmoPosture?: string | null;
  /** Distribution list of email addresses copied from the receiving unit at request time, used to pre-fill the schedule email recipients. Pre-existing malformed entries are still possible for older units and are filtered out client-side before opening the mailto draft. */
  unitDistroEmails?: string[];
  /** CC distribution list copied from the receiving unit at request time, used to pre-fill the cc= field of the schedule email. Pre-existing malformed entries are still possible for older units and are filtered out client-side before opening the mailto draft. */
  unitDistroCcEmails?: string[];
  /** BCC distribution list copied from the receiving unit at request time, used to pre-fill the bcc= field of the schedule email (silent recipients hidden from To/CC). Pre-existing malformed entries are still possible for older units and are filtered out client-side before opening the mailto draft. */
  unitDistroBccEmails?: string[];
  baselineId?: string | null;
  horizonDays: number;
  burnModel: string;
  safetyMarginDays: number;
  resupplyLeadDays: number;
  publishedAt?: string | null;
  shareToken?: string | null;
  createdAt?: string;
  events: ScheduleEventItem[];
}

export interface ShareLinkRevokeResult {
  scheduleId: string;
  /** Always null after a successful revoke. */
  shareToken: string | null;
  revoked: boolean;
}

export interface ShareLinkRotateResult {
  scheduleId: string;
  /** The newly minted share token. Distribute this in place of the old one. */
  shareToken: string;
}

export interface OpsecPushTile {
  scheduleId: string;
  scheduleLabel: string;
  unitId: string;
  unitName: string;
  publishedAt?: string | null;
  horizonDays: number;
  nextPushDate?: string | null;
  totalPushes: number;
  shareToken?: string | null;
}

/**
 * What MARLOG surface produced the proposal payload:
- calculator: ad-hoc requirements bill from the standalone calculator
- schedule: a published comms-denied schedule (resupply events)
- supply: a unit's current on-hand supply state (with overrides)

 */
export type SpirePrSourceKind =
  (typeof SpirePrSourceKind)[keyof typeof SpirePrSourceKind];

export const SpirePrSourceKind = {
  calculator: "calculator",
  schedule: "schedule",
  supply: "supply",
} as const;

export interface SpirePrConfig {
  configured: boolean;
  owner?: string | null;
  name?: string | null;
  baseBranch?: string | null;
  message?: string | null;
}

/**
 * Calculator scenario inputs. Required when sourceKind=calculator.
 */
export type CreateSpirePrBodyScenario = {
  /** @minimum 1 */
  personnel: number;
  /** @minimum 1 */
  days: number;
  climate: Climate;
  opTempo: OpTempo;
};

export interface CreateSpirePrBody {
  sourceKind: SpirePrSourceKind;
  /** Unit ID for calculator/supply, schedule ID for schedule. */
  sourceId?: string;
  /** Calculator scenario inputs. Required when sourceKind=calculator. */
  scenario?: CreateSpirePrBodyScenario;
  /**
   * Custom PR title. Defaults to a MARLOG-generated title.
   * @maxLength 120
   */
  title?: string;
  /**
   * Optional markdown body appended to the PR description.
   * @maxLength 4000
   */
  summary?: string;
  /**
   * Who is opening the PR (free-form, e.g. callsign).
   * @maxLength 120
   */
  createdBy?: string;
}

export type SpirePrPayloadSummary = { [key: string]: unknown } | null;

export interface SpirePr {
  id: string;
  sourceKind: SpirePrSourceKind;
  sourceId?: string | null;
  sourceLabel: string;
  repoOwner: string;
  repoName: string;
  branch: string;
  baseBranch: string;
  filePath: string;
  prNumber: number;
  prUrl: string;
  title: string;
  /** open | closed | merged */
  state: string;
  mergedAt?: string | null;
  closedAt?: string | null;
  createdBy?: string | null;
  createdAt: string;
  refreshedAt: string;
  payloadSummary?: SpirePrPayloadSummary;
}

export type UpdateCommsHygieneSettings400 = {
  error: string;
};

export type ListCommsHygieneRunsParams = {
  /**
   * @minimum 1
   * @maximum 200
   */
  limit?: number;
};

export type GetSupplyHistoryParams = {
  days?: number;
  itemId?: string;
};

export type ListSpirePrsParams = {
  sourceKind?: SpirePrSourceKind;
  sourceId?: string;
  /**
   * @minimum 1
   * @maximum 200
   */
  limit?: number;
  /**
   * When true, re-poll GitHub for the state of any rows still marked open before returning.
   */
  refresh?: boolean;
};
